package NewFunctions;

/**
 * @author Joachim
 */
public class findWidestCoverage {
    
    public void findWidestCoverage(){
        //airline_nodes;
        //tmp_airlines; 
        //tmp_airline;
        //tmp_max_coverage = 0;
        /*
        for (airline a : all_airlines) {
            airports_covered, _ = runPrimsOnAirline(a)
            if (airports_covered.length > tmp_max_coverage) {
                tmp_max_coverage = airports_covered.length;
                tmp_airports = airports_covered;
                tmp_airline = a;
        }
        
        System.out.println("There are " + airline_nodes.keys.length + " different airlines);
        return tmp_airline, tmp_max_coverage;
        */
        
    }
}
